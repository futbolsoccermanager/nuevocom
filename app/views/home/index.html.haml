- if user_signed_in? && current_user.mis_selecciones.present?
  .dashboard
    = render 'dashboard'
- else
  %h1= t('index.titulo')
  %p= t('index.parrafo1')
  %p= t('index.parrafo2')
  %p= t('index.parrafo3')
